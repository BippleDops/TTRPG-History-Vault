# Python dependencies for the tooling in Scripts/ (MIT licensed, see Scripts/LICENSE).
# Install with:  python3 -m pip install -r requirements.txt
#
# The validators (link_validator.py, schema_validator.py, reciprocal_link_checker.py)
# need only PyYAML; the analytics and export scripts need the rest.
pyyaml>=6
matplotlib>=3.8
pandas>=2.0
numpy>=1.26
scipy>=1.11
seaborn>=0.13
plotly>=5
networkx>=3
requests>=2.31
